# React 账单项目教学文档

## 目录
1. [项目整体介绍](#项目整体介绍)
2. [项目结构说明](#项目结构说明)
3. [JavaScript 基础知识](#javascript-基础知识)
4. [React 基础知识](#react-基础知识)
5. [前后端交互](#前后端交互)
6. [核心代码解析](#核心代码解析)

---

## 项目整体介绍

这是一个**记账应用**，类似于支付宝或微信的记账功能。用户可以：
- 记录收入和支出
- 查看月度账单统计
- 查看年度账单统计

**技术栈：**
- **前端**：React（一个流行的 JavaScript 框架）
- **UI 组件库**：antd-mobile（专门为移动端设计的组件库）
- **状态管理**：Redux（管理应用中的数据）
- **后端**：json-server（一个模拟的后端服务器）
- **路由**：react-router-dom（管理页面跳转）

---

## 项目结构说明

```
react-bill-test/
├── public/              # 静态资源文件夹（图片、HTML等）
├── server/              # 后端数据文件夹
│   └── data.json     # 存储账单数据
├── src/                # 源代码文件夹
│   ├── components/     # 全局组件（可以在任何页面使用）
│   │   └── Icon/     # 图标组件
│   ├── contants/       # 常量定义（固定的数据）
│   ├── pages/          # 页面组件
│   │   ├── Layout/   # 布局页面（包含底部导航栏）
│   │   ├── Month/    # 月度账单页面
│   │   │   └── components/  # Month 页面专用的组件
│   │   ├── New/      # 记账页面
│   │   └── Year/     # 年度账单页面
│   ├── router/         # 路由配置
│   ├── store/          # Redux 状态管理
│   ├── App.js          # 根组件
│   ├── index.js         # 入口文件
│   └── theme.css       # 主题样式
└── package.json        # 项目配置文件
```

**为什么这样组织？**
- **components**：存放可以在多个页面复用的组件，避免重复代码
- **pages**：每个页面一个文件夹，方便管理
- **store**：统一管理应用的数据，任何页面都能访问
- **router**：集中管理所有页面的跳转规则

---

## JavaScript 基础知识

### 1. 变量声明

```javascript
// 旧写法（不推荐）
var name = '张三';
let age = 18;

// 新写法（推荐）
const name = '张三';  // const 声明的变量不能重新赋值
let age = 18;          // let 声明的变量可以重新赋值
```

**为什么要用 const 和 let？**
- `const`：声明后不会改变的变量（比如常量），更安全
- `let`：声明后会改变的变量（比如计数器）
- 避免使用 `var`，因为它有作用域问题，容易出错

### 2. 箭头函数

```javascript
// 传统函数写法
function add(a, b) {
  return a + b;
}

// 箭头函数写法（推荐）
const add = (a, b) => {
  return a + b;
}

// 如果函数只有一行，可以简写
const add = (a, b) => a + b;
```

**为什么要用箭头函数？**
- 语法更简洁
- 自动绑定 `this`，不会出现 `this` 指向错误的问题
- 在 React 中使用更方便

### 3. 解构赋值

```javascript
// 假设有一个对象
const user = {
  name: '张三',
  age: 18,
  city: '北京'
};

// 传统写法
const name = user.name;
const age = user.age;

// 解构写法（推荐）
const { name, age, city } = user;
console.log(name);  // 输出：张三
console.log(age);   // 输出：18
```

**为什么要用解构？**
- 代码更简洁
- 不需要重复写 `user.xxx`
- 在 React 中非常常用

### 4. 数组方法

```javascript
// map：遍历数组，返回新数组
const numbers = [1, 2, 3, 4];
const doubled = numbers.map(num => num * 2);
console.log(doubled);  // [2, 4, 6, 8]

// filter：过滤数组，返回符合条件的元素
const numbers = [1, 2, 3, 4, 5, 6];
const evenNumbers = numbers.filter(num => num % 2 === 0);
console.log(evenNumbers);  // [2, 4, 6]

// reduce：累加数组，返回一个值
const numbers = [1, 2, 3, 4];
const sum = numbers.reduce((total, num) => total + num, 0);
console.log(sum);  // 10
```

**在项目中的应用：**
- `map`：渲染账单列表
- `filter`：筛选支出或收入的账单
- `reduce`：计算总支出或总收入

---

## React 基础知识

### 1. 什么是 React？

React 是一个**用于构建用户界面的 JavaScript 库**。

**核心思想：**
- **组件化**：把页面拆分成一个个小组件
- **声明式**：你只需要描述界面长什么样，React 会帮你更新界面
- **单向数据流**：数据从上往下传递

### 2. JSX 语法

JSX 是 JavaScript 的扩展，允许在 JavaScript 中写类似 HTML 的代码。

```javascript
// 普通 HTML
<div>
  <h1>标题</h1>
  <p>内容</p>
</div>

// JSX（在 JavaScript 中写 HTML）
const element = (
  <div>
    <h1>标题</h1>
    <p>内容</p>
  </div>
);
```

**注意事项：**
- `class` 要写成 `className`（因为 `class` 是 JavaScript 保留字）
- 标签必须闭合（`<div></div>` 或 `<div />`）
- 可以在 `{}` 中写 JavaScript 表达式

```javascript
// 在 JSX 中使用 JavaScript
const name = '张三';
const age = 18;

<div>
  <h1>你好，{name}</h1>  {/* 输出：你好，张三 */}
  <p>你今年{age}岁了</p>   {/* 输出：你今年18岁了 */}
</div>
```

### 3. 组件

组件是 React 的核心概念，可以理解为**可复用的 UI 片段**。

```javascript
// 函数组件（推荐）
function Welcome() {
  return <h1>欢迎</h1>;
}

// 或者用箭头函数
const Welcome = () => {
  return <h1>欢迎</h1>;
};

// 使用组件
<div>
  <Welcome />
  <Welcome />
</div>
```

### 4. Props（属性）

Props 是**父组件传递给子组件的数据**。

```javascript
// 子组件
function UserCard({ name, age, city }) {
  return (
    <div>
      <h1>{name}</h1>
      <p>年龄：{age}</p>
      <p>城市：{city}</p>
    </div>
  );
}

// 父组件
function App() {
  return (
    <div>
      <UserCard name="张三" age={18} city="北京" />
      <UserCard name="李四" age={20} city="上海" />
    </div>
  );
}
```

**为什么要用 Props？**
- 让组件更灵活，可以复用
- 父组件控制子组件显示的内容

### 5. State（状态）

State 是**组件内部的数据，数据变化时界面会自动更新**。

```javascript
import { useState } from 'react';

function Counter() {
  // 声明一个状态，初始值为 0
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>当前计数：{count}</p>
      <button onClick={() => setCount(count + 1)}>
        点击加 1
      </button>
    </div>
  );
}
```

**解读：**
- `useState(0)`：创建一个状态，初始值是 0
- `count`：当前的状态值（只读，不能直接修改）
- `setCount`：修改状态的函数（必须通过这个函数修改）
- `setCount(count + 1)`：修改状态，React 会自动重新渲染界面

**为什么不能直接修改 count？**
```javascript
// ❌ 错误写法
count = count + 1;  // React 不知道你修改了，界面不会更新

// ✅ 正确写法
setCount(count + 1);  // React 知道你修改了，会更新界面
```

### 6. useEffect（副作用）

useEffect 用于**处理副作用**，比如数据获取、订阅、定时器等。

```javascript
import { useState, useEffect } from 'react';

function UserList() {
  const [users, setUsers] = useState([]);

  // 组件加载后执行一次
  useEffect(() => {
    // 模拟获取数据
    fetch('https://api.example.com/users')
      .then(res => res.json())
      .then(data => setUsers(data));
  }, []);  // 空数组表示只执行一次

  return (
    <ul>
      {users.map(user => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
}
```

**解读：**
- 第一个参数：要执行的函数
- 第二个参数：依赖数组（空数组表示只在组件挂载时执行一次）
- 当依赖数组中的值变化时，会重新执行

### 7. 条件渲染

根据条件显示不同的内容。

```javascript
function Welcome({ isLoggedIn }) {
  return (
    <div>
      {isLoggedIn ? (
        <h1>欢迎回来！</h1>
      ) : (
        <h1>请先登录</h1>
      )}
    </div>
  );
}
```

**解读：**
- `isLoggedIn ? ... : ...`：三元运算符
- `?` 前面是条件成立时显示的内容
- `:` 后面是条件不成立时显示的内容

### 8. 列表渲染

使用 `map` 方法渲染列表。

```javascript
function TodoList() {
  const todos = [
    { id: 1, text: '学习 React' },
    { id: 2, text: '写代码' },
    { id: 3, text: '休息' }
  ];

  return (
    <ul>
      {todos.map(todo => (
        <li key={todo.id}>
          {todo.text}
        </li>
      ))}
    </ul>
  );
}
```

**为什么要加 key？**
- React 用 key 来识别哪个元素改变了
- 必须是唯一的（通常是 id）
- 没有 key 会导致性能问题和警告

---

## 前后端交互

### 1. 什么是前端和后端？

**前端：**
- 用户直接看到和操作的界面
- 运行在浏览器中
- 负责：显示数据、收集用户输入、页面跳转

**后端：**
- 服务器端，处理业务逻辑
- 负责数据存储、数据查询、权限验证
- 本项目使用 json-server 模拟后端

### 2. HTTP 请求

前端通过 HTTP 请求与后端通信。

**常用方法：**
- `GET`：获取数据（查询账单）
- `POST`：创建数据（新增账单）
- `PUT`：更新数据（修改账单）
- `DELETE`：删除数据（删除账单）

### 3. Axios 库

Axios 是一个**用于发送 HTTP 请求的库**，比原生的 `fetch` 更好用。

```javascript
import axios from 'axios';

// GET 请求（获取数据）
axios.get('http://localhost:8888/ka')
  .then(response => {
    console.log(response.data);  // 获取到的数据
  })
  .catch(error => {
    console.error('请求失败', error);
  });

// POST 请求（创建数据）
const newBill = {
  type: 'pay',
  money: -100,
  date: '2024-01-10',
  useFor: 'food'
};

axios.post('http://localhost:8888/ka', newBill)
  .then(response => {
    console.log('创建成功', response.data);
  })
  .catch(error => {
    console.error('创建失败', error);
  });
```

**为什么要用 Axios？**
- 自动转换 JSON 数据
- 支持请求和响应拦截器
- 更好的错误处理
- 支持浏览器和 Node.js

### 4. async/await

async/await 是**处理异步操作的语法糖**，让异步代码看起来像同步代码。

```javascript
// 传统写法（Promise 链式调用）
function getBillList() {
  axios.get('http://localhost:8888/ka')
    .then(response => {
      return response.data;
    })
    .then(data => {
      console.log(data);
    })
    .catch(error => {
      console.error(error);
    });
}

// async/await 写法（推荐）
async function getBillList() {
  try {
    const response = await axios.get('http://localhost:8888/ka');
    const data = response.data;
    console.log(data);
  } catch (error) {
    console.error(error);
  }
}
```

**为什么要用 async/await？**
- 代码更易读，像同步代码一样
- 错误处理更方便（用 try/catch）
- 在 React 中非常常用

---

## 核心代码解析

### 1. src/index.js（入口文件）

```javascript
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import sum from '@/test';
import { RouterProvider } from 'react-router-dom';
import router from '@/router';
import { Provider } from 'react-redux';
import store from './store';

// 导入定制主题文件
import './theme.css';

const total = sum(1, 3);
console.log(total);

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <Provider store={store}>
    <RouterProvider router={router} />
  </Provider>
);
```

**逐行解读：**

1. `import React from 'react';`
   - 导入 React 库，这是所有 React 组件的基础

2. `import ReactDOM from 'react-dom/client';`
   - 导入 ReactDOM，用于将 React 组件渲染到页面

3. `import './index.css';`
   - 导入全局样式文件

4. `import sum from '@/test';`
   - 导入一个自定义函数（测试用）
   - `@/` 是路径别名，代表 `src/` 目录

5. `import { RouterProvider } from 'react-router-dom';`
   - 导入路由提供者，用于管理页面跳转

6. `import router from '@/router';`
   - 导入路由配置（定义了哪些路径对应哪些页面）

7. `import { Provider } from 'react-redux';`
   - 导入 Redux 提供者，用于管理全局状态

8. `import store from './store';`
   - 导入 Redux store（存储所有应用数据的地方）

9. `const total = sum(1, 3);`
   - 调用测试函数，计算 1 + 3 = 4

10. `const root = ReactDOM.createRoot(document.getElementById('root'));`
    - 创建 React 根节点，找到页面中 id 为 root 的元素

11. `root.render(<Provider store={store}><RouterProvider router={router} /></Provider>);`
    - 渲染应用
    - `Provider`：让所有组件都能访问 Redux store
    - `RouterProvider`：让所有组件都能使用路由功能

**为什么要这样写？**
- 这是 React 应用的标准入口写法
- `Provider` 包裹整个应用，确保所有组件都能访问状态
- `RouterProvider` 包裹整个应用，确保所有组件都能跳转页面

---

### 2. src/router/index.js（路由配置）

```javascript
import Month from '@/pages/Month'
import Year from '@/pages/Year'
import Layout from '@/pages/Layout'
import New from '@/pages/New'
import { createBrowserRouter } from 'react-router-dom';
import { Navigate } from 'react-router-dom';

const router = createBrowserRouter([
  {
    path: '/',
    element: <Layout />,
    children: [
      {
        path: '',
        element: <Navigate to="/month" replace />
      },
      {
        path: 'month',
        element: <Month />
      },
      {
        path: 'year',
        element: <Year />
      }
    ]
  },
  {
    path: '/new',
    element: <New />
  }
])

export default router;
```

**逐行解读：**

1. `import Month from '@/pages/Month';`
   - 导入月度账单页面组件

2. `import Year from '@/pages/Year';`
   - 导入年度账单页面组件

3. `import Layout from '@/pages/Layout';`
   - 导入布局组件（包含底部导航栏）

4. `import New from '@/pages/New';`
   - 导入记账页面组件

5. `import { createBrowserRouter } from 'react-router-dom';`
   - 导入创建路由的函数

6. `import { Navigate } from 'react-router-dom';`
   - 导入导航组件，用于重定向

7. `const router = createBrowserRouter([...]);`
   - 创建路由实例，传入路由配置数组

8. `{ path: '/', element: <Layout />, children: [...] }`
   - 定义根路径 `/`
   - 使用 `Layout` 组件（显示底部导航栏）
   - `children`：定义子路由（在 Layout 内部显示的页面）

9. `{ path: '', element: <Navigate to="/month" replace /> }`
   - 空路径表示访问根路径 `/`
   - 自动重定向到 `/month` 页面
   - `replace`：替换历史记录，用户不能按返回键回到根路径

10. `{ path: 'month', element: <Month /> }`
    - 定义 `/month` 路径
    - 显示 `Month` 组件

11. `{ path: '/new', element: <New /> }`
    - 定义 `/new` 路径（独立页面，不在 Layout 内）
    - 显示 `New` 组件

**为什么要这样配置？**
- 嵌套路由：`Layout` 包裹子路由，子路由共享底部导航栏
- 独立路由：`/new` 不在 `Layout` 内，没有底部导航栏
- 默认重定向：访问根路径自动跳转到月度账单，提升用户体验

---

### 3. src/store/modules/billstore.js（Redux 状态管理）

```javascript
import { createSlice } from '@reduxjs/toolkit';
import axios from 'axios';

const billStore = createSlice({
  name: 'bill',
  initialState: {
    billList: []
  },
  reducers: {
    setBillList(state, action) {
      state.billList = action.payload;
    }
  }
});

const { setBillList } = billStore.actions;

const getBillList = () => {
  return async (dispatch) => {
    const res = await axios.get('http://localhost:8888/ka');
    dispatch(setBillList(res.data));
  }
};

const addBillList = (data) => {
  return async (dispatch) => {
    const res = await axios.post('http://localhost:8888/ka', data);
    dispatch(setBillList(res.data));
  }
};

export { getBillList, addBillList };
const reducer = billStore.reducer;
export default reducer;
```

**逐行解读：**

1. `import { createSlice } from '@reduxjs/toolkit';`
   - 导入创建 Redux slice 的函数
   - slice 是 Redux Toolkit 的概念，包含状态和修改状态的方法

2. `import axios from 'axios';`
   - 导入 axios，用于发送 HTTP 请求

3. `const billStore = createSlice({...});`
   - 创建一个 Redux slice
   - 包含状态和修改状态的方法

4. `name: 'bill',`
   - 定义 slice 的名称，用于生成 action 类型

5. `initialState: { billList: [] },`
   - 定义初始状态
   - `billList`：存储账单列表，初始为空数组

6. `reducers: { setBillList(state, action) { ... } }`
   - 定义同步修改状态的方法
   - `state`：当前的状态
   - `action`：包含要修改的数据

7. `state.billList = action.payload;`
   - 直接修改状态
   - `action.payload`：包含新数据

8. `const { setBillList } = billStore.actions;`
   - 解构出 action 创建函数
   - 用于在组件中调用

9. `const getBillList = () => { return async (dispatch) => { ... } };`
   - 定义一个异步 action（thunk）
   - 用于获取账单列表

10. `const res = await axios.get('http://localhost:8888/ka');`
    - 发送 GET 请求到后端
    - 等待响应

11. `dispatch(setBillList(res.data));`
    - 调用同步 action 更新状态
    - 将获取到的数据存入 store

12. `const addBillList = (data) => { ... };`
    - 定义添加账单的异步 action

13. `const res = await axios.post('http://localhost:8888/ka', data);`
    - 发送 POST 请求到后端
    - 传递新账单数据

**为什么要用 Redux？**
- 全局状态管理：任何页面都能访问账单数据
- 数据流清晰：数据只能通过 action 修改，易于调试
- 时间旅行：可以撤销/重做操作（虽然本项目没用到）

---

### 4. src/pages/Month/index.js（月度账单页面）

```javascript
import { NavBar, DatePicker } from 'antd-mobile';
import { useState, useEffect } from "react";
import './index.scss';
import classNames from 'classnames';
import dayjs from 'dayjs';
import { useSelector } from 'react-redux';
import { useMemo } from 'react';
import _ from 'lodash';
import Daybill from './components/DayBill';

const Month = () => {
  const billList = useSelector(state => state.bill.billList);
  const monthGroup = useMemo(() => {
    return _.groupBy(billList, (item) => dayjs(item.date).format('YYYY-MM'));
  }, [billList]);

  const [dateVisible, setDateVisible] = useState(false);
  const [currentDate, setCurrentDate] = useState(() => {
    return '2023-03';
  });
  const [currentMonthList, setCurrentMonthList] = useState([]);

  const monthResult = useMemo(() => {
    const pay = currentMonthList.filter(item => item.type === 'pay').reduce((a, c) => a + c.money, 0);
    const income = currentMonthList.filter(item => item.type === 'income').reduce((a, c) => a + c.money, 0);
    return {
      pay,
      income,
      total: pay + income
    };
  }, [currentMonthList]);

  useEffect(() => {
    const availableMonths = Object.keys(monthGroup);
    if (availableMonths.length > 0) {
      const firstMonth = availableMonths[0];
      setCurrentDate(firstMonth);
      setCurrentMonthList(monthGroup[firstMonth]);
    }
  }, [monthGroup]);

  const onConfirm = (date) => {
    setDateVisible(false);
    const formatDate = dayjs(date).format('YYYY-MM');
    setCurrentMonthList(monthGroup[formatDate] || []);
    setCurrentDate(formatDate);
  };

  const dayGroup = useMemo(() => {
    const groupData = _.groupBy(currentMonthList, (item) => dayjs(item.date).format('YYYY-MM-DD'));
    const keys = Object.keys(groupData);
    return {
      groupData,
      keys
    };
  }, [currentMonthList]);

  return (
    <div className='monthlyBill'>
      <NavBar className='nav' backArrow={false}>
        月度收支
      </NavBar>
      <div className='content'>
        <div className='header'>
          <div className='date' onClick={() => setDateVisible(true)}>
            <span className='text'>{currentDate + ' '}月账单</span>
            <span className={classNames('arrow', dateVisible && 'expand')}></span>
          </div>
          <div className='twoLineOverview'>
            <div className='item'>
              <span className='money'>{monthResult.pay.toFixed(2)}</span>
              <span className='type'>支出</span>
            </div>
            <div className='item'>
              <span className='money'>{monthResult.income.toFixed(2)}</span>
              <span className='type'>收入</span>
            </div>
            <div className='item'>
              <span className='money'>{monthResult.total.toFixed(2)}</span>
              <span className='type'>结余</span>
            </div>
          </div>
          <DatePicker
            className='kaDate'
            title='记账日期'
            precision='month'
            visible={dateVisible}
            onCancel={() => setDateVisible(false)}
            onConfirm={onConfirm}
            onClose={() => setDateVisible(false)}
            max={new Date()}
          />
        </div>
        <div className='dailyList'>
          {
            dayGroup.keys.map(key => {
              return <Daybill key={key} date={key} billList={dayGroup.groupData[key]} />
            })
          }
        </div>
      </div>
    </div>
  )
}

export default Month;
```

**逐行解读：**

1. `import { NavBar, DatePicker } from 'antd-mobile';`
   - 导入导航栏和日期选择器组件

2. `import { useState, useEffect } from "react";`
   - 导入 React Hooks

3. `import classNames from 'classnames';`
   - 导入类名工具，用于动态添加 CSS 类

4. `import dayjs from 'dayjs';`
   - 导入日期处理库

5. `import { useSelector } from 'react-redux';`
   - 导入从 Redux store 读取数据的 hook

6. `import { useMemo } from 'react';`
   - 导入性能优化 hook

7. `import _ from 'lodash';`
   - 导入工具函数库

8. `const billList = useSelector(state => state.bill.billList);`
   - 从 Redux store 读取账单列表
   - `state.bill.billList`：访问 bill slice 中的 billList 状态

9. `const monthGroup = useMemo(() => { ... }, [billList]);`
   - 按月分组账单
   - 使用 lodash 的 `groupBy` 方法
   - `useMemo`：只有 billList 变化时才重新计算，提升性能

10. `dayjs(item.date).format('YYYY-MM')`
    - 格式化日期为年-月格式（如：2023-03）

11. `const [dateVisible, setDateVisible] = useState(false);`
    - 控制日期选择器的显示/隐藏

12. `const [currentDate, setCurrentDate] = useState(() => { return '2023-03'; });`
    - 当前选中的月份
    - 使用函数初始化，避免每次渲染都重新计算

13. `const [currentMonthList, setCurrentMonthList] = useState([]);`
    - 当前月份的账单列表

14. `const monthResult = useMemo(() => { ... }, [currentMonthList]);`
    - 计算当前月份的收支统计

15. `currentMonthList.filter(item => item.type === 'pay').reduce((a, c) => a + c.money, 0);`
    - 筛选支出账单并累加金额

16. `currentMonthList.filter(item => item.type === 'income').reduce((a, c) => a + c.money, 0);`
    - 筛选收入账单并累加金额

17. `useEffect(() => { ... }, [monthGroup]);`
    - 当 monthGroup 变化时执行
    - 初始化显示第一个有数据的月份

18. `const onConfirm = (date) => { ... };`
    - 日期选择器的确认回调

19. `dayjs(date).format('YYYY-MM')`
    - 格式化选中的日期

20. `setCurrentMonthList(monthGroup[formatDate] || []);`
    - 设置当前月份的账单列表
    - `|| []`：如果该月没有数据，设置为空数组

21. `const dayGroup = useMemo(() => { ... }, [currentMonthList]);`
    - 按日分组当前月份的账单

22. `dayGroup.keys.map(key => { return <Daybill ... /> })`
    - 遍历每一天，渲染单日账单组件

23. `<NavBar className='nav' backArrow={false}>`
    - 导航栏组件
    - `backArrow={false}`：不显示返回箭头

24. `<DatePicker visible={dateVisible} onConfirm={onConfirm} ... />`
    - 日期选择器组件
    - `visible`：控制显示/隐藏
    - `onConfirm`：确认选择时的回调

**为什么要这样写？**
- `useMemo`：避免不必要的计算，提升性能
- `useSelector`：从 Redux 读取数据，自动订阅更新
- `useState`：管理组件内部状态
- `useEffect`：处理副作用（初始化数据）

---

### 5. src/pages/New/index.js（记账页面）

```javascript
import { Button, DatePicker, Input, NavBar } from 'antd-mobile';
import Icon from '@/components/Icon';
import './index.scss';
import classNames from 'classnames';
import { billListData } from '@/contants';
import { useNavigate } from 'react-router-dom';
import { useState } from 'react';
import { useDispatch } from 'react-redux';
import { addBillList } from '@/store/modules/billstore';

const New = () => {
  const navigate = useNavigate();
  const [money, setMoney] = useState(0);
  const moneyChange = (value) => {
    setMoney(value);
  }
  const [billType, setBillType] = useState('pay');
  const [useFor, setUseFor] = useState(' ');
  const dispatch = useDispatch();

  const saveBill = () => {
    const data = {
      type: billType,
      money: billType === 'pay' ? -money : +money,
      date: new Date(),
      useFor: useFor
    };
    console.log(data);
    dispatch(addBillList(data));
  };

  return (
    <div className="keepAccounts">
      <NavBar className="nav" onBack={() => navigate(-1)}>
        记一笔
      </NavBar>
      <div className="header">
        <div className="kaType">
          <Button
            shape="rounded"
            className={classNames(billType === 'pay' ? 'selected' : '')}
            onClick={() => setBillType('pay')}
          >
            支出
          </Button>
          <Button
            className={classNames(billType === 'income' ? 'selected' : '')}
            shape="rounded"
            onClick={() => setBillType('income')}
          >
            收入
          </Button>
        </div>
        <div className="kaFormWrapper">
          <div className="kaForm">
            <div className="date">
              <Icon type="calendar" className="icon" />
              <span className="text">{'今天'}</span>
              <DatePicker
                className="kaDate"
                title="记账日期"
                max={new Date()}
              />
            </div>
            <div className="kaInput">
              <Input
                className="input"
                placeholder="0.00"
                type="number"
                value={money}
                onChange={moneyChange}
              />
              <span className="iconYuan">¥</span>
            </div>
          </div>
        </div>
      </div>
      <div className="kaTypeList">
        {billListData[billType].map(item => {
          return (
            <div className="kaType" key={item.type}>
              <div className="title">{item.name}</div>
              <div className="list">
                {item.list.map(item => {
                  return (
                    <div
                      className={classNames('item', '')}
                      key={item.type}
                      onClick={() => setUseFor(item.type)}
                    >
                      <div className="icon">
                        <Icon type={item.type} />
                      </div>
                      <div className="text">{item.name}</div>
                    </div>
                  )
                })}
              </div>
            </div>
          )
        })}
      </div>
      <div className="btns">
        <Button className="btn save" onClick={saveBill}>
          保 存
        </Button>
      </div>
    </div>
  )
}

export default New;
```

**逐行解读：**

1. `import { useNavigate } from 'react-router-dom';`
   - 导入导航 hook，用于页面跳转

2. `import { useDispatch } from 'react-redux';`
   - 导入 dispatch hook，用于调用 Redux action

3. `import { addBillList } from '@/store/modules/billstore';`
   - 导入添加账单的 action

4. `const navigate = useNavigate();`
   - 创建导航函数

5. `const [money, setMoney] = useState(0);`
   - 金额状态，初始值为 0

6. `const moneyChange = (value) => { setMoney(value); };`
   - 金额变化处理函数

7. `const [billType, setBillType] = useState('pay');`
   - 账单类型状态（pay-支出，income-收入）

8. `const [useFor, setUseFor] = useState(' ');`
   - 账单用途状态（如：food、drinks）

9. `const dispatch = useDispatch();`
   - 创建 dispatch 函数，用于调用 Redux action

10. `const saveBill = () => { ... };`
    - 保存账单函数

11. `const data = { type: billType, money: billType === 'pay' ? -money : +money, date: new Date(), useFor: useFor };`
    - 构建账单数据对象
    - 支出金额为负数，收入金额为正数

12. `dispatch(addBillList(data));`
    - 调用 Redux action，添加账单

13. `<NavBar onBack={() => navigate(-1)}>`
    - 导航栏，点击返回按钮时返回上一页

14. `<Button onClick={() => setBillType('pay')}>`
    - 支出按钮，点击时设置类型为 pay

15. `<Input value={money} onChange={moneyChange} />`
    - 金额输入框
    - `value`：绑定金额状态
    - `onChange`：金额变化时更新状态

16. `{billListData[billType].map(item => { ... })}`
    - 根据账单类型显示对应的账单类型列表

17. `onClick={() => setUseFor(item.type)}`
    - 点击账单类型时，设置用途

18. `<Button onClick={saveBill}>`
    - 保存按钮，点击时调用保存函数

**为什么要这样写？**
- 受控组件：输入框的值由 state 控制，确保数据一致性
- 事件处理：通过函数处理事件，保持代码清晰
- Redux：通过 dispatch 更新全局状态，所有页面都能看到新数据

---

## 学习建议

### 1. 循序渐进

1. **第一阶段**：理解 JavaScript 基础
   - 变量、函数、数组、对象
   - ES6+ 新特性（箭头函数、解构、模板字符串）

2. **第二阶段**：学习 React 基础
   - JSX 语法
   - 组件、Props、State
   - 事件处理

3. **第三阶段**：学习 React Hooks
   - useState、useEffect、useMemo
   - 自定义 Hooks

4. **第四阶段**：学习路由和状态管理
   - react-router-dom
   - Redux

5. **第五阶段**：学习前后端交互
   - HTTP 请求
   - async/await
   - Axios

### 2. 实践建议

1. **多写代码**：理论不如实践，多写才能掌握
2. **阅读官方文档**：React、Redux、Ant Design Mobile 的文档都很详细
3. **看源码**：阅读开源项目的源码，学习别人的写法
4. **调试**：学会使用浏览器开发者工具，调试代码
5. **提问**：遇到问题不要怕，多问多查

### 3. 常见问题

**Q1：为什么我的组件不渲染？**
A：检查以下几点：
- 组件是否正确导出（`export default`）
- 组件是否正确导入
- 是否有语法错误（查看控制台）

**Q2：为什么我的状态不更新？**
A：检查以下几点：
- 是否通过 `setState` 修改状态（不能直接修改）
- 是否在正确的函数中调用 `setState`
- 是否有异步问题（在回调中修改状态）

**Q3：为什么我的请求失败？**
A：检查以下几点：
- 后端服务器是否启动
- 请求地址是否正确
- 跨域问题（后端需要配置 CORS）

---

## 总结

这个项目涵盖了：
- ✅ React 组件开发
- ✅ 状态管理（Redux）
- ✅ 路由管理（React Router）
- ✅ 前后端交互（Axios）
- ✅ UI 组件库（Ant Design Mobile）
- ✅ 样式管理（SCSS）

通过学习这个项目，你将掌握：
1. React 的核心概念和最佳实践
2. JavaScript 的现代语法和特性
3. 前后端交互的基本原理
4. 大型应用的组织方式

**继续加油！编程是一个不断学习的过程，遇到问题不要怕，多查多问多练！**
